# .env.example — copy to .env and fill in values.
# Never commit .env to version control.
# See DECISIONS.md → Environment Variables Required for
# the full list of variables this project needs.

# Database (required for Drizzle migrations)
DATABASE_URL=
MISTRAL_API_KEY=
AGENT_ID=
NODE_ENV=
HOST=
PORT=

# Add project-specific variables below.
# Names are listed in DECISIONS.md — values belong only in .env.